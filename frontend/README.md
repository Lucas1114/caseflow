# CaseFlow Frontend

React and TypeScript client for CaseFlow. During development, Vite proxies `/api` requests to the Spring Boot backend at `http://localhost:8080`.

Run `npm install` once, then use `npm run dev` from this directory.
