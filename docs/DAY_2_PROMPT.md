# CaseFlow Day 2 Prompt

Use this prompt to start a new Codex task named `Day 2`:

```text
We are continuing the CaseFlow 7-day full-stack portfolio sprint.

This is the Day 2 task. The repository is located at:

/Users/lucas/Projects/caseflow

Before making any changes:

1. Read AGENTS.md completely.
2. Read docs/CASEFLOW_PROJECT.md completely and treat it as the source of truth.
3. Inspect Git status, recent commits, and the current repository structure.
4. Verify that Day 1 is complete and identify the existing React -> GET /api/cases -> Spring Boot -> PostgreSQL flow.
5. Briefly summarize the current architecture in plain language.

Working rules:

- Work incrementally and explain each meaningful step before large changes.
- Do not generate the whole Day 2 implementation at once.
- Prefer simple commercial-style solutions over sophisticated architecture.
- Keep Java/Spring Boot straightforward.
- Explain React and TypeScript concepts carefully as they appear, including components, props, hooks, state, types, TanStack Query, and API integration.
- PostgreSQL must continue to run only through Docker Compose.
- Do not add authentication, authorization, Redis, Kafka, Kubernetes, CQRS, event sourcing, microservices, AI, complex observability, or performance work unless explicitly approved.
- Keep README.md and docs/CASEFLOW_PROJECT.md synchronized at meaningful milestones.
- Do not begin Day 3.

First task:

Based on the verified Day 1 application, propose a focused Day 2 scope with one clear completion criterion, the exact backend and frontend changes it would require, and a small-step execution sequence. Keep the proposal realistic for one sprint day and portfolio-focused.

Do not implement Day 2 until I approve the proposed scope.
```
